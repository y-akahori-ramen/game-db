cdk.out/
node_modules/

# Compiled output of lib/ and bin/ TypeScript sources (tsc compiles in place, no outDir).
# NOTE: infra/lambda/**/*.js are hand-written Lambda source files, not compiled output —
# do not broaden these patterns to match them.
/lib/**/*.js
/lib/**/*.js.map
/lib/**/*.d.ts
/bin/**/*.js
/bin/**/*.js.map
/bin/**/*.d.ts

__pycache__/
*.pyc
